# Only needed to build a standalone .app/.exe -- not required to run the
# app from source. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-build.txt
pyinstaller>=6.0
